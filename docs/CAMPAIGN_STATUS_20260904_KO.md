# GPU RASBERY 처리량·정확도 캠페인 현황 (2026-09-04)

인용용 통합본. 모든 수치는 출처(리포트 블록 번호 / 문서 절 / 메모리 항목 일시)를 달았다.
출처 약어: **B##** = `E:\rasbery_runs\2026-08-30\238\pricing_388e8f2.md`의 블록 ##,
**M(일시)** = 메모리 `gpu-dispatch-branch-verdict.md`의 해당 일시 항목,
그 외는 `docs/` 문서 절 이름. 호스트는 별칭(**238** = Xeon+GPU 서버, **181** = RTX PRO 6000 워크스테이션)만 쓴다.

## 1. 공식 수치

### 1.1 정확도 (production 봉투, 불변)

| 지표 | 값 | 출처 |
|---|---|---|
| max\|Δkeff\| | **1.847 pcm** (1.847~1.906 범위) | B52 Gate B(C/E1/E2 동일), M(§2) |
| max\|Δppm\| | **15.334 ppm** (15.309~15.334) | B52 |
| max\|Δao\| / max\|Δfqp\| / max\|Δfrp\| | 0.012 / 0.071 / 0.017 | B52 |
| BOC 핀 RMS / max | **0.238 % / 0.80 %** | B52 gate_b_pinrms |
| 정본 digest / outers | `1f36e75dc00ed2b4` / **4,377** | B52 arm A·B, M(§2) |

### 1.2 속도 — 프리셋별 (KNGR 단일 = warm+hot3 중앙값, 배치 = 256잡 매니페스트)

| 아암 | outers/sp | 단일 wall | vs MASTER 27.2 s | 배치 8×M8 | 배치 8×M16 | Gate B |
|---|---:|---:|---:|---:|---:|---|
| production(strict) | 125.1 (B57) / 125.06 (B56) | **9.378 s** (B49v2 c1, B50) | **2.9×** | 1,208 (B57) / 1,191 (B56) | **1,440.1 c/h** (B39c, B50) = **6.6× W16 217 c/h** | 1.85 pcm / 15.3 ppm / 0.238 % |
| screen100 (100 pcm 봉투) | 77.2 (B57) / 77.17 (B56) | **7.67 s** (B57) | 3.5× | **1,638** (B57) / 1,615 (B56) | — | **9.0 pcm**, 14.8 ppm, 핀 **0.237 %**/0.81 % (B57·B56) |
| screen100x (xe_tol 1e-4) | **60.3** (B57) | **6.80 s** (B57) | **4.0×** | **2,046** (B57) | **2,225 c/h** (B57) = **10.3× W16** | **9.3 pcm**, 핀 0.237 % (B57) |
| screen100x + `A2_PREV_INNER=carry` | **43.9** (B57) | **6.17 s** (B57) | 4.4× | 미측정 | 미측정 | 9.2 pcm (B57) |

- screen100 아암은 **screen100 봉투**(100 pcm / 33.5 ppm / 핀 1.0 %)로 채점한다. production 봉투로 재면 당연히 FAIL — B56이 명시(§screen100 Gate B).
- screen100x는 별도 봉투가 아니라 `gate_b_envelope.py`의 **별칭**이다(WP24 §7.5).
- B56의 절대 wall(strict 13.407 s / screen100 11.225 s)은 `OUTER_SEGMENT_V2` 누락 env라 **9.378 s와 직접 비교 금지**(B52 항목 (c), B56 CAVEAT). 프리셋 간 비율(단일 1.194×, 배치 1.356×)만 유효.
- 배치 청정 앵커: 8×M16+MPS **1,438.0 c/h**(B52 bestarm 1,438.0/1,435.3/1,440.9), 51v2 자체 중앙값 1,400.0 — 2.6 % 이내 정합(B51v2).

### 1.3 자체 CPU 대비 (같은 커밋·같은 호스트, B50)

| 비교 | CPU | GPU | 배수 |
|---|---:|---:|---:|
| KNGR 단일, OMP=1 | 124.697 s | 9.378 s | **13.30×** |
| KNGR 단일, OMP=12 | 42.421 s | 9.378 s | 4.52× |
| m64 배치 (8인스턴스×3스레드) | 34.2 c/h | 1,440.1 c/h | **~42.1×** |

### 1.4 i-SMR 기준선

- **HIGA CY01 MASTER 단일 wall = 26.26 s**(181, warm+10회 중앙값, min 25.55/max 26.51; step-1 K-EFF 1.032934 vs 기준 1.032935 = 0.1 pcm) — ISMR 런북 부록 A. W16 c/h는 권한 차단으로 미측정.

## 2. 20× 경로 — 남은 1.95×

목표 4,340 c/h ÷ 현재 최고 2,225 c/h = **1.95×**(B57 8×M16 screen100x 기준).

**남은 레버**
1. **carry 배치 측정** — 단일에서 60.3 → 43.9 outers/sp(−27 %)인데 배치 c/h 미측정(B57). 57b에 큐잉(M 09-04 21:00).
2. **형상 재탐색** — 51v2 순위 8×M16 1,400 > 4×M32 1,295 > 8×M8 ~1,212(n=2) > 16×M8 1,172 > 2×M64 912;
   8×M32·4×M64·16×M16·8×M32_cmfd32는 전부 OOM(B51v2). 128 슬롯이 현 카드의 실질 천장 — screen100x의 낮은 메모리 압력에서 재탐색 여지.
3. **호스트 측 부담** — 51c(워커당 OMP=24/procs + `OMP_WAIT_POLICY=PASSIVE`, rolling A/B, `BATCH_WAIT_US` 100/400/1000/3000 스윕)는 오염으로 무효(M 09-04 11:00) → **청정 게이트 후 재실행 대기**.
4. **A2 D1/D3** — Xe 캐스케이드가 여전히 남은 outer의 최대 덩어리(screen100 xe 1,847 → screen100x 1,316, B57·M 09-04 02:10/01:00). D1 상태점 seed, D3 미실행.
5. **커널 dispatch-bound** — CMFD 커널 grid 34/67/17/1 블록 대 188 SM ≈ 18 % 실점유, 2.5~4.3 µs(M 09-04 00:20 refill 조사). 더 넓은 gather + WP17 CMFD_BLOCK/persistent.

**정리된(확정/철회) 가설**
- **즉시 refill 1.35~1.7× → 철회.** width_fill 0.36은 빈 슬롯이 아니라 케이스 간 위상 편차이고 슬롯 점유는 이미 93~96 % → 실이득 **1.04~1.07×**(M 09-04 00:20). WP18 rolling refill도 c/h 불변(M §4 WP18).
- **"배치는 AA-off" → 오해로 정정.** 배치 워커 로그 실측 anderson_accepted 38~39k, txn_steps ~60k = **AA-on**. 하네스 `DEFAULT_ENV`가 `RASBERY_XE_ANDERSON=1`을 내보낸다(M 09-04 03:00, 케이스키 문서 §2). 숨은 대형 이득 없음.
- **"+8.5 % 코드 회귀" → 철회.** bisect 결과 b69d7d4 10.73 / 22f8148 9.95 / 7aab350 10.01 s, 셋 다 `1f36e75d`/4377 → 커밋 간 회귀 없음, 기준 대비 +6~14 %는 호스트 노이즈(M 09-04 04:30).
- **MPS 잔여 설정·GPU 클럭 → 배제.** 클럭 2,317~2,422 MHz, throttle 0x0, ECC·mtime 정상(M 09-04 01:30, B 텔레메트리 절).
- **`--batch-mode 1` 함정.** 이 argv 플래그가 Xe AA 기본값을 뒤집어 같은 env로 digest `4c663ff538b28f82`/**7,087 outers**를 만든다(정상 단일은 `1f36e75d`/4,377). 벤치·bisect 스크립트에서 제거(M 09-04 02:00/03:00, 케이스키 문서 §1).
- **분모 정의 정리(GAP 문서 §1.2)**: MASTER 단일 27.2 s 대비 2.90×, MASTER W16 217 c/h 대비 6.66×, **자체 CPU-only 175.83 s(같은 GPU 서버) 대비 18.75×** — "20×"가 성립하는 유일한 정의는 후자이며 이미 1.07× 이내.
  구조적 상한: 커널이 wall의 34.8~37.8 %이므로 GPU를 무한히 빠르게 해도 **Amdahl 1.53~1.61×**(§1.3).

## 3. 정확성 계약 (이번 주기 수정)

- **케이스 키 v4까지 4회 이동.** v2 = `exec_mode` + 실효 `xe_anderson`/`xe_txn` + `forms_digest` (`[CASE]` schema 8, `[TRAJECTORY]` schema 2; 케이스키 문서 §2) → v3 = `th_fuel_rods`(schema 9; TH 봉수 문서)
  → v4 = `th_tf_table = <name>:<sha256>`(schema 10; TF표 문서). **매번 전 캐시 재키잉 필요.**
- **FORMS_PIN.** 호스트별로 갈리는 채굴 마스크(WSL 0x6 / 238 0x7)가 어떤 env 문자열로도 표현되지 않던 구멍 → `src/GpuFormMask.h` 레지스트리 + `RASBERY_FORMS_PIN=default|mined`(기본 mined), `Driver::primeFormMasks()`가
  키 계산 직전 선해소(케이스키 문서 §3·§5). 55b 확인: `FORMS_PIN=default` → `7b12f60d`/4374(M 09-04 13:00).
- **B55b 재검증 결과**: 배치-모드-1 ≠ 단일 ≠ 하네스 워커로 키가 정상 분기, B0 `1f36e75d`/4377 유지(M 09-04 13:00). `[CASE]`가 plain single을 AA off로 보고하던 건은 `35ea39f`가 게이트에 `mode_declared` 기록 + stderr 경고로 처리(키 불변).
- **FP32 arm 복구.** B55에서 F1~F4 전부 rc=139 segfault. 원인 = `22f8148` narrowNonFinite가 그래프 캡처 중 `cudaEventQuery` 호출 → 캡처 무효화 → `CudaOuterGraph.cu:2192` 실패. `731d6ec`가 `cudaStreamIsCapturing` 판정으로 수정,
  55b에서 **F1~F5 전부 rc=0**(F1 `add78049`/4434, F2 `8a1be88d`/4425, F3=F4 `e91ac290`/4439, F5 `3a29add4`/4415)(M 09-04 00:10/13:00).
- **CUDA=OFF 링크 복구.** B55 `CudaBICGBackendStub.cpp:90` 선언 불일치 → `731d6ec`에서 5인자 `enqueueSweeps` 등 스텁 보강, 55b에서 **ON/OFF 양쪽 빌드 OK, ctest 15/15**(M 09-04 13:00).
- 미해결: B56 SS7.3의 2건 FAIL은 238 g++가 `-std=c++20` 미인식(`c++2a`)인 툴체인 문제 — `d9f2c65`가 폴백 프로브 추가(B56, M 09-04 02:30).

## 4. TH 결함 두 건

### 4.1 노드당 연료봉 수 (해결, 검증 대기)

- 연료온도 선출력 `lpd = 1000·P/(RODS·hz)`의 `RODS`가 **62.0 리터럴로 3곳 하드코딩** (`src/XSSet.cpp:6378`, `src/ThKernel.h:436`, `src/ThReference.cpp:209`; CPU 기준선 e76d40d 상속) — TH 봉수 문서 §결함.
- 실제: APR1400 16×16 = 236/4 = **59**, i-SMR 17×17 = 260/4 = **65**. i-SMR 상승분 비 실측 1.047~1.062 ≈ 65/62 = 1.048 → tfavg **+9 K**(≈ −25 pcm), APR1400은 반대 부호(59/62 = 0.952).
- 수정 `51fb1bf`: `RASBERY_TH_FUEL_RODS=legacy|deck|<n>`, 덱 키 `geometry.dimensions.nfrod`, `[TH][NFROD]` 영수증.
  **기본 legacy=62 → B0 보존.** 238 블록 58 큐잉(M 09-04 07:00).

### 4.2 TF 테이블 노형 불일치 (회귀 완료, 검증 대기)

- `include/Database/tf.csv`는 MASTER 내장 **WH형**(isolth=11)인데 APR1400/KNGR은 **CE형**(isolth=12) — TF표 문서 §발견.
- 결과: 연소도 기울기가 ~5배 완만(16 GWd/t 구간 상승분 CE **−29.6 %** vs WH **−5.6 %**) →
  KNGR tfavg Δ가 BOC **−14.6 K** → EOC **+71 K**로 흐르고, 이를 붕소 탐색이 흡수해
  **Gate B의 −15.3 ppm 잔차가 사실상 이 결함이라는 가설**(Δfqp −0.057 동반) — TF표 문서 §발견.
- `d7c5061`: 181 MASTER KNGR `$TF` 편집 런(118.5 s, 기준과 bit-identical)에서 `$FB2D`/`$P2D`/`$B2D` 축평균
  **9,640 샘플 / 40 상태점**을 회귀 → `tf_ce.csv`, **잔차 RMS 2.2252 K / max 11.887 K**, LPD 단조성 위반 0건.
  `parse_master_rst.py`로 MAS_RST 레이아웃 역공학 — **레코드 23/24/25에 온도가 없다**(전부 0) → 3-D 노드 회귀 불가.
- 캐비앗: 축평균 Jensen 편향, 90 격자점 중 **41개가 표본 0**이라 `--relative-to`로 WH 형상 유지(외삽), LPD ≤ ~354 W/cm 커버.
- 합격 기준(TF표 문서 §정확성 분류): 전 상태점 `|Δtfavg| ≤ 5 K`, `max|Δppm| ≤ 5`, `|Δfqp| ≤ 0.02`.
  238 **블록 59**(legacy / deck-nfrod / deck+CE 3팔) 대기(M 09-04 12:00).

## 5. i-SMR MASTER 비교

- **앵커 A**(GPU vs CPU 트윈): CY01 max\|Δkeff\| **0.0920 pcm**(한도 5), CY02 0.0231 pcm, ppm/AO/핀 전부 0.0000,
  h5diff 0줄 ×3 — INSIDE(B54).
- **앵커 B**(MASTER 임계 봉 위치 고정 Δρ, block54c/54d):

  | 주기 | 이력 교정 전 RMS | 54d(CY01 rod-critical) 후 RMS | rod-cm max |
  |---|---:|---:|---:|
  | CY02 | 521 pcm (max 709) | **145** | 41 → **8.9 cm** |
  | CY03 | 213 (max 480) | **135** | 25.3 |
  | CY04 | 111 (max 261) | **137** | 3.3 |

  → CY01 감손 이력 불일치가 CY02 초과분의 대부분을 설명, 잔여 ~135~145 pcm/주기는 주기 내 드리프트로 남음
  (M 09-04 04:30/05:00/20:00). 접힌 맵 BOC 연소도 RMS **1.72 → 0.22 GWd/t**(M 09-04 21:00).
- **1.96 d 첫 감손 스텝 스파이크** +205~266 pcm(54d 후 CY02 +226), 19.6 d에 소멸, 3주기 공통.
  MASTER는 `XE/SM='1 2'`(Sm transient)이나 RASBERY엔 Sm 모드가 없고 Pm-149(T½ 53 h) 첫 스텝 CRAM 수치로 추정 →
  **54e 첫 스텝 세분화 실험 대기**(M 09-04 05:30/06:30).
- **TH 편향**: RASBERY 연료온도 +8~12 °C 계통 편향(≈ −25 pcm, 부호 반대 → 독립 결함) = §4.1(M 09-04 05:30).
- **반사체 YG3**: 주변부 편향 없음(맵 근거) → 원인 순위 하위(M 09-04 05:30).
- **회전 폴드 파티션은 버그 아님**(`853635d`, 회전셔플 문서 §판정): 90° 폴드에서 (0,c)/(c,0)은 한 물리 집합체의
  두 반쪽이며 제안된 교환은 질량을 깬다(2칸 중복·2칸 소실). `%LPD_SHF` 33항목 전부 row,col 순서로 일치.
  잔여 11.983/16.260 vs MASTER 16.385는 **비교 지표 규약 문제 + 이월 이력 결손** → `33fbcd6`의
  `compare_assembly_maps.py`(반쪽 슬롯 → 물리 집합체 접기)로 처리.
- **접힌 집합체 출력 ΔRMS/max %**(54d 이력 교정 **전**, M 09-04 10:00): CY02 BOC 2.3/7.1 · MOC 5.3/15.8 · EOC 2.2/5.0;
  CY03 5.7/12.8 · 1.1/3.9 · 1.6/3.8; CY04 1.2/3.5 · 1.7/4.7 · 1.0/2.6.
- **단일 속도(GPU vs CPU-12, B54)**: cy02_step1 1.633/2.043 = 1.25×, i-SMR_CY02 6.980/11.237 = **1.61×**,
  i-SMR_CY01 2.801/3.816 = 1.36× — 소형 노심이라 GPU 이득이 작다.
- **24케이스 배치**(8×M8+MPS, 12 rod×8 shuffle×4 boron): 중앙값 **539.6 c/h**(534.4/542.2/539.6). B54의
  raw TOTAL 3,859~3,897 c/h는 24잡 매니페스트의 다른 분모 규약이므로 **인용 금지**(B54 자체 주석).

## 6. 미해결·대기

1. **51c 재실행** — GPU-free + loadavg1 < 4가 3분 지속하는 게이트 통과 후, 수신증에 load 기록(M 09-04 11:00).
2. **54b (HIGA 앵커 C)** — 3차 재시도 성공(M 09-04 20:00)이나 **핀 비교와 26.26 s 대비 GPU wall 미보고**. `--chiffoni` 절대경로(`60d3275`) 반영.
3. **54e** — Pm/Sm 첫 스텝 세분화. 스테이징 심링크 누락으로 재실행 대기(M 09-04 21:00).
4. **블록 58 / 59** — `RASBERY_TH_FUEL_RODS`(58), TF 표 3팔 사다리(59). 둘 다 재실행 대기.
5. **tf 표 3-D 보정** — Jensen 편향 제거에는 MASTER의 3-D 피드백 맵이 필요(`iprfb=2`는 `$FB1D`/`$FB2D`만 인쇄).
   41개 외삽 격자점을 KNGR이 실제로 밟는지 (bu, lpd) 히스토그램으로 확인(TF표 문서 §미해결).
6. **MASTER W16 c/h** — 181 로컬 권한 분류기가 16 동시 프로세스 기동을 차단, 미측정(ISMR 런북 부록 A).
7. **198 실행 권한** — 유휴·스테이징 완료이나 원격 실행이 분류기에 차단(M 09-03 i-SMR 항목).
8. **B55 soak rc=1** — `soak_report.json`은 기록됐으나 종료코드 비-0, 미해독(B55).
9. **재실행 큐(fix_reruns)** — 스테이징 덱 상대경로 누락(`kngr_chiffon_bppm.h5`, `restart_23.h5`)(M 09-04 20:00).

## 7. 운영 규칙 (요약)

- **호스트**: 238 = GPU0 속도측정 전용 / GPU1 게이트·soak 전용(GPU1 사용 중 GPU0 측정 금지). 181 = 빌드·컴파일 검증 + MASTER 실행. 198/199는 이 작업에 사용 불가. **IP 주소는 기록하지 않는다.**
- **로컬 PC 계산·컴파일 금지.** C:는 코드만, run 결과·빌드 산출물은 E:.
- **타 사용자 작업 불가침** — 청정 대기(load < 4, GPU 유휴)로 판정하고 `block##_DONE` 마커로 체인.
- **벤치마크 위생**: 배치 수치는 반드시 ≥3회 중앙값(단일 run 산포 ±26 %), 벤치 전 고아 프로세스 확인,
  MASTER 비교는 `RASBERY_PPR_MODE=master` 포함 env parity 필수(누락 시 핀 RMS 0.522/2.13 %로 오염 — 실제 회귀 아님).
- **골든 규약**: 단일 500, 배치 708.
- **게이트 우선순위**: Gate A(공학 스크린)는 1차 필터, **Gate B(MASTER 직접비교)가 항상 우선**.
  등급 B0(bit-exact) > N1(ULP급 궤적 이동, project_acceptance 없이는 HOLD) > A2(알고리즘 근사, NEVER_DEFAULT·항상 opt-in).
- **프리셋 아암은 `--set-unset` 3줄(`STAGED_FLUX_TOL`/`STAGED_XE_TOL`/`STAGED_LOOSE_SETTLE`) 필수** —
  없으면 env 다이제스트가 `DEFAULT_ENV` 값을 접어 아암 간 케이스 키가 비교되지 않는다(WP24 §7.1/§7.2).
- **장시간 감시**는 Monitor 도구(Bash 백그라운드 상한 600 s).
