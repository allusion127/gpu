# WP19.2–WP23 범위 적대적 리뷰·수정 보고 (2026-09-03)

리뷰 대상 `0054838..ed6b5dc`. 원시 지적 64 → 고유 63 → 3-렌즈 반박 검증 후 확정 46 → critical/high 18건 패치 → **16건 착지**(2건은 기착지 수정 `3b7ed88`/`b69d7d4`가 상위 호환이라 폐기). 착지 커밋 `03ed6aa..22f8148`, 181 컴파일 `[100%] Built target RASBERY`, 계약 테스트 전부 PASS.

# WP19.2–WP23 범위 리뷰 보고서 (`0054838..ed6b5dc`)

## 1. CONFIRMED 지적 사항 (중복 차원 통합, 28건)

| # | file:line | 심각도 | 요지 | 수정 |
|---|---|---|---|---|
| 1 | src/CudaCramBackend.h:209/243 · src/XSSet.cpp:5374 | high | `mic_device`를 `const void*[11]`로 좁혔으나 `FillCramMicDevice(const double**)` 미변경 → 구간 전체 빌드 불가 | ✔ 배치1–10 |
| 2 | src/CudaCramBackend.cu:1116 | high | `mic_device_elem_bytes` 생산자 없음 → `kCramWidenMic` 도달 불가, widen 경로는 xfer 원장 미기록 | ✔ 배치1–10 |
| 3 | src/CudaCramBackendStub.cpp:37 | high | WP21-D 접근자 4종 미정의 → CPU-only 빌드 링크 실패 | ✔ 배치1–10 |
| 4 | src/GpuFp32Arm.h:287–288 | high | `converted(Cram)=false`로 CRAM 분기 사장, `"declined"` 상태·demotion 카운트 모두 도달 불가한데 case key만 분기 | ✔ 배치1–10 |
| 5 | src/GpuFp32Arm.h:376 | high | FP32 CMFD 커널 가동 중에도 영수증이 `cmfd:"fp64"` 보고 | ✔ 배치1–10 |
| 6 | src/CudaBICGBackend.cu:403 | high | `RASBERY_GPU_CMFD_FP32` 단독으로 FP32 커널 진입, 영수증은 `arm:"fp64"` | ✔ `3b7ed88` |
| 7 | src/CudaBICGBackend.cu:402 | medium | 프로세스 latch를 `cmfdFp32InnerEnabled()` 함수 static이 우회 | ✔ 배치1–10 |
| 8 | src/CudaBICGBackend.cu:6218 | high | FP32 latch가 프로세스 전역·sticky → 상주 evaluator가 FP32 case_key로 FP64 답을 제공 | ✔ `73a0b73` |
| 9 | src/CudaBICGBackend.cu:6175 | high | 비유한 valve가 element 단위 가드/slot 단위 플래그 불일치 → 부분 갱신 flux를 채택 | ✔ 배치1–10 |
| 10 | src/FlatXsCtaKernel.cuh:317 | high | CTA float 워크스페이스가 전역 FP64 `v.iden`에 float 반올림 값 기록 | ✔ 배치1–10 |
| 11 | src/GpuFp32Arm.h:443 | high | `RASBERY_GPU_FP32_PPR` 파싱 부재 → Arm B/D 동일 바이너리 | ✔ `9baa3ed` |
| 12 | src/CudaBICGBackend.cu:2421 · :2722 | high | `fp32_refine_rounds`가 미초기화 slot·test-only 라운드를 계수 → 기본 cap에서 평균 2.0 고정 | ✔ `406452c` |
| 13 | src/CudaXsReconBackend.cu:6490 | high | narrow nodal 비유한 valve가 device-outer-segment arm에서 도달 불가 | ✔ `22f8148` |
| 14 | src/CudaXsReconBackend.cu:4025 | high | SENM consts 자체검사가 non-blocking 스트림 커널 미동기 상태로 D2H 판독 | ✖ 이미 `b69d7d4`로 해소 |
| 15 | src/CudaXsReconBackend.cu:4043 | medium | float 양자화 피연산자에 double ULP 척도 사용 → FP32에서 자체검사 무력 | ✔ 배치1–10 |
| 16 | src/CudaXsReconBackend.cu:5794 | medium | `nodal_stage_event`가 WP23 arm에서 영구 null → pinned staging 재사용 가드 부재 | ✔ 배치1–10 |
| 17 | src/GpuDeviceBlockPool.h:399 | high | 풀 eviction의 `cudaFree`가 AllocWindow 밖 → WP19 capture 위험 재개방 | ✔ `9e9117f` |
| 18 | src/GpuDeviceBlockPool.h:148 | medium | `parked` 무한 증가·eviction 부재 → 상주 캠페인에서 VRAM 누수 | ✔ 배치1–10 |
| 19 | src/GpuDeviceBlockPool.h:22 | medium | "eviction은 곧 finding" 주장을 강제하는 게이트 없음 | ✔ 배치1–10 |
| 20 | src/CrashReport.h:310 | high | 시그널 핸들러가 `captureThreadOrdinal()`(thread_local 동적 초기화+atomic RMW) 호출, 선언된 async-signal-safe 집합 위반 | ✔ `6ef4e38` |
| 21 | src/CrashReport.h:299–303 | medium | CAS 패자가 즉시 re-raise → 승자 기록 중간 절단; 기록이 ~25회 비버퍼 write로 16레인 공유 파이프에 분할 | ✔ 배치1–10 |
| 22 | src/EvaluatorServer.h:1750 | high | `arena_rebuilds`의 3/4 호출부가 per-case 블록 재성장 → "steady state 0" 불변식 성립 불가 | ✔ `1435917` |
| 23 | src/EvaluatorServer.h:2234 | medium | `race_spanned`가 프로세스 전역 델타 → 무관한 레인의 실패까지 재실행 허용 | ✔ 배치1–10 |
| 24 | src/EvaluatorServer.h:1543 | medium | phase가 매 케이스 `capture_race_retry`로 고정, 나머지 3개 phase 미사용 | ✔ 배치1–10 |
| 25 | src/EvaluatorServer.h:1620 | low | isolation recheck 경로에 `crash::Scope` 없음 → 빈 크래시 기록 | ✔ 배치1–10 |
| 26 | tools/run_multi_gpu_batch.py:1023 · :1900 | medium | backtrace 캡처가 무한정·형제 레인 stdout 혼입; 3개 사망 경로 중 1곳만 배선 | ✔ 배치1–10 |
| 27 | tools/soak_run.py:156 · :1149 | medium | `GPU_FULL_FALLBACKS`가 WP22/23 신규 4개 누락; 재시작 복구 후 wall_s·c/h가 복구 wave만 반영 | ✔ 배치1–10 |
| 28 | src/Driver.h:1983 · src/CmfdAssemblyKernel.h:40 · docs/WP21_B2C2:160 | low–med | RESIDENCY JSON 미이스케이프; `kLayoutVersion`은 graph key 아닌 영수증 전용; 타일 기본값 문서 4/8 vs 실제 2/2 | ✔ 배치1–10 |

## 2. REFUTED: 6건 (예시 3)

- **CRAM widen 경로의 "그럴듯한 오답 인벤토리"** — `narrow` 항상 false·`kCramWidenMic` 도달 불가라는 정적 사실은 맞으나, float 블록에서 double 길이 D2D를 하는 실패 시나리오는 성립하지 않음(#2로 축소 재분류).
- **"FP32 flat-XS가 FP64 CRAM이 소비하는 전역 블록을 오염"** — 기전은 실재하나 defect 프레이밍이 틀림. tip에서 `FlatXsKernel.h`에 float 포인터가 없고 `FillCramMicDevice` 경로가 주장한 소비 사슬을 만들지 않음(#10로 재정식화).
- **WP21-A pack 레인의 미배수 enqueue 경로 순서 위반** — 필요한 경로가 커밋 상태에 부재. 모든 `packMat`/`packCpl` 호출부가 `issueSweepUploads`의 배수 경로에만 존재.
- (외 3건: WP21-D B0 pole 상수 전제 오류 — 두 arm 모두 `__constant__` 메모리로 컴파일타임 리터럴 아님; flat-XS latch 부재 추론; race_spanned 중복 제기.)

## 3. 통합 결과

- 브랜치 `codex/exact-throughput-campaign`, **푸시 없음**. 도착 시 트리는 `b57b3f7`(패치 1–5, 7–10 선행 커밋 완료), 미추적 스크래치 5개 외 clean.
- 이번 세션 신규 7커밋: `73a0b73`(11), `6ef4e38`(12), `1435917`(13), `9e9117f`(15), `9baa3ed`(16), `406452c`(17), `22f8148`(18). 총 18건 중 **16건 적용**.
- **미적용 2건**: 패치 6은 이미 커밋된 패치 2(`3b7ed88`)와 상호배타적 설계(전자는 `armedFor()` 통합·`envFlagEnabled()` 제거, 후자는 별도 `noteExternalArm`+4번째 상태어 추가)이며 `tools/test_gpu_fp32_contract.py:725`/`:197`의 계약과 정면 충돌 — 의도는 `3b7ed88`이 완전 보존. 패치 14는 `b69d7d4`가 더 강한 형태(`xfer::streamSync(... "selfcheck drain")` + 계약 테스트 `r_self_check_ordered`)로 선반영.

## 4. 238 러너 재검증 항목

1. **빌드 게이트**: CUDA on/off 양쪽 전체 재빌드(#1·#3) — CPU-only 스텁 링크 포함.
2. **FP32 arm 매트릭스**: `RASBERY_GPU_FP32` / `_CRAM` / `_STRICT` / `_PPR` / 레거시 `RASBERY_GPU_CMFD_FP32` 조합에서 `[RASBERY][FP32]` 영수증의 `arm`/`cmfd`/`ppr` 문자열과 실제 커널 집합 일치, `bytes_saved_est` 단위, case key 분기 여부.
3. **CRAM/XS 블록**: `[RASBERY][CRAM_GPU]` 신규 4필드, mic D2D의 XFER 원장 대사, `[RASBERY][CMFD][GRAPH]` census.
4. **정밀도 게이트**: nodal consts ULP 자체검사(FP32 arm 포함), segment-arm에서 jnet 비유한 valve 발화, `fp32_refine_rounds/_solves` 평균 재측정(기존 2.0 고정 값 폐기).
5. **상주 soak**: latch 이후 case_key 정합, `arena_rebuilds`=0, `pool_evictions`/`pool_park_refusals` 게이트, 장시간 VRAM 추이(#18).
6. **크래시 하네스**: 의도적 SIGSEGV 주입으로 16레인 하 기록 무결성·`crash_evidence` 3개 사망 경로·`race_spanned` 레인 스코프 확인.
7. **영수증 파서**: RESIDENCY JSON 이스케이프, `GPU_FULL_FALLBACKS` 신규 4서브시스템, 재시작 복구 세대의 wall_s/c-h.

## 부록. 통합 로그

STATE ON ARRIVAL: the working tree was already at `b57b3f7`, clean apart from five pre-existing untracked scratch files, with patches 1–5 and 7–10 of the 18 already committed by an earlier run of this task. I continued from there: re-verified the one previously-skipped patch, then applied 11–18.

COMMITS CREATED THIS SESSION (branch `codex/exact-throughput-campaign`, nothing pushed):

| # | hash | finding |
|---|---|---|
| 11 | `73a0b73` | FP32 latch is process-wide and sticky, so a resident evaluator serves FP64 answers under an FP32 case_key |
| 12 | `6ef4e38` | signal handler calls captureThreadOrdinal(), running a thread_local dynamic initializer + atomic RMW inside the signal |
| 13 | `1435917` | arena_rebuilds counts per-case XsRecon block regrowths |
| 15 | `9e9117f` | WP10.8 pool eviction calls cudaFree outside any AllocWindow |
| 16 | `9baa3ed` | RASBERY_GPU_FP32_PPR is never parsed |
| 17 | `406452c` | finalize_status reports a refinement round for slots whose counters were never zeroed |
| 18 | `22f8148` | narrow nodal non-finite valve unreachable on the production segment arm |

Already committed before this session (patches 1–5, 7–10): `03ed6aa`, `3b7ed88`, `b457693`, `4fe06d9`, `c7d8948`, `3f643f2`, `6eea427`, `face6aa`, `b57b3f7`.

PATCHES NOT APPLIED (2)

**Patch 6 — "RASBERY_GPU_CMFD_FP32 alone runs the FP32 kernel set while the receipt reports arm:\"fp64\"".** `git apply --3way` → *"Applied patch to 'src/CudaBICGBackend.cu' with conflicts. Applied patch to 'src/GpuFp32Arm.h' with conflicts."* It is an alternative fix for the same defect as patch 2, which is already committed as `3b7ed88`, and the two designs are mutually exclusive, not mergeable:
- patch 2 folded the legacy knob into `rasbery::fp32::armedFor()` and deleted `envFlagEnabled()` from the CMFD TU; the gate is now a bare `routes(Cmfd)`. Patch 6's pre-image no longer exists anywhere.
- `backendState()` already reads `armedFor(which)`; the receipt's `arm` is already three-valued (`fp32`/`partial`/`fp64`). Patch 6 would add a *second*, unset `noteExternalArm` flag and a fourth word `external`.
- Directly contradictory contract rules: `tools/test_gpu_fp32_contract.py:725` (from patch 2) asserts the CMFD gate contains **no** `envFlagEnabled(`, no `getenv`, no `||` — exactly what patch 6 reintroduces; line 197 asserts `routes()` does not call `armed()`, which patch 6's negative control relies on. Applying both cannot pass. Intent is fully preserved by `3b7ed88`.

**Patch 14 — "WP23 SENM-consts self-check reads the device block while the kernel is still running".** `git apply --3way` → conflicts in `src/CudaXsReconBackend.cu`, `src/NodalConstsReceipt.h`, `tools/test_nodal_consts_gpu_contract.py`. The defect is already fixed in-tree by `b69d7d4`, in a strictly stronger form: `src/CudaXsReconBackend.cu:4043` has `xfer::streamSync("CudaXsReconBackend.cu:nodalConstsOnDevice", "selfcheck drain", stream)` before the read; `r_self_check_ordered` in the contract test additionally asserts the sync *precedes* the first `cudaMemcpyDeviceToHost` and has its own negative control (drain retargeted to `nullptr`); `kNodalConstsPolicyNote` and §6 of the KO doc already state the ordering. Patch 14 would add a redundant second sync and rename the ledger leaf back to `"selfcheck"`, weakening the existing rule.

HAND-RESOLVED CONFLICTS (2)

- **Patch 11**, `tools/test_gpu_fp32_contract.py`: both sides appended to the `NEGATIVES` list. Resolved keeping both blocks in order.
- **Patch 16**, hand-merged against already-committed `b57b3f7`, which pinned `scope_gated_backends(ARM) == ["Cram"]` on the premise that CRAM was the only doubly-gated backend. Adding PPR's gate makes that `["Cram", "Ppr"]`, and three of `b57b3f7`'s negative controls stopped firing (mutating only the CRAM half left PPR converted-and-gated, so the property still held). All three were widened to mutate both gates, and a fourth was added for the new gate's own liveness (`converted(Ppr) → false`). The header prose in `src/GpuFp32Arm.h` claiming "CRAM is the only one gated twice" was corrected in the same commit.

TEST RESULTS (all on the final tree, all rc=0)

Required list: `test_enum_alias_contract` PASS (157 files, 74 enums) · `test_dependent_template_contract` PASS (155 files) · `test_xfer_ledger_contract` PASSED · `test_gpu_fp32_contract` PASS (447 checks, 42 negative controls) · `test_cmfd_soa_contract` PASS · `test_nodal_soa_contract` PASS · `test_evaluator_residency_contract` PASS.

Touched by the patches: `test_micx_resident_contract` OK (12 checks, 19 controls) · `test_cmfd_fp32_contract` PASS (7 rollback controls, all fired) · `test_cmfd_fuse_contract` PASS · `test_cram_gpu_contract` PASS (15 rules) · `test_crash_report_contract` PASSED · `test_arena_persist_contract` PASS (9 controls, 15 compiled ×2 arms) · `test_device_block_pool_contract` PASS (13 controls, 14 compiled assertions ×2 arms) · `test_nodal_consts_gpu_contract` PASS (15 rules) · `test_capture_arbiter_contract` PASSED.

Adjacent, run because the patches move their inputs: `test_evaluator_contract`, `test_soak_receipt_schema_contract`, `test_ppr_gpu_master_mode_contract`, `test_case_key_contract`, `test_cram_parallel_contract`, `test_flatxs_cta_contract` — all PASS.

The **pre-existing FAIL** of `test_capture_arbiter_contract`'s "every allocation is inside the arbiter" (`src/GpuDeviceBlockPool.h:498`, reported in the patch-15 notes as present at the base) is closed by `9e9117f` and the suite is green.

COMPILE CHECK ON 181: `git diff ed6b5dc..HEAD` copied to `~/patches/review18.patch`, applied `--3way` onto a worktree at `ed6b5dc`, configured Release / `RASBERY_ENABLE_CUDA=ON` / `CMAKE_CUDA_ARCHITECTURES=120` and built to `[100%] Built target RASBERY`. Only pre-existing "style of line directive is a GCC extension" nvcc-stub warnings. As the earlier notes record, cmake there needs `-DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc` explicitly — nvcc is not on that shell's PATH. Remote worktree `~/wt_review18` and the patch file were removed afterwards; two unrelated worktrees at `d515dde` belonging to other agents were left alone.

NOTES
- No compilers, solvers or benchmarks were run on this PC. `tools/test_arena_persist_contract.py` and `tools/test_device_block_pool_contract.py` do shell out to a local `g++` for their own single-TU harnesses (their output says "compiled ×2 arms"); they are on the whitelisted `tools/test_*_contract.py` list, but flagging it in case that was not intended.
- Nothing was pushed. Working tree is clean apart from the same five pre-existing untracked files.
- Patch files: `C:/Users/MK/AppData/Local/Temp/claude/c--Users-MK-Desktop-CT-RPL-4-Codes-0-GPU/04b51a28-b9f0-4c6e-95ed-a770b2316da8/scratchpad/p18/` (`p06_external_arm.patch`, `p11_latch_case_boundary.patch`, `p12_crash_handler_reads.patch`, `p13_arena_rebuild_naming.patch`, `p14_selfcheck_stream_sync.patch`, `p15_evict_alloc_window.patch`, `p16_ppr_flag_is_read.patch`, `p16b_ppr_flag_contract.patch`, `p17_refine_rounds_absent.patch`, `p18_nodal_valve_on_device.patch`, `p18b_nodal_valve_contract.patch`, `full_ed6b5dc_to_head.patch`).
- Patches 16, 17 and 18 were hand-adapted to the current tree, so their `@@` counts were regenerated by `git apply --recount` rather than being byte-copies of the supplied hunk headers.
